---
phase: 06-api-polish-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/API_DOCS.md
autonomous: true
requirements: [INFRA-01]
user_setup: []
must_haves:
  truths:
    - "API_DOCS.md documents all 10 endpoints (Auth, Articles, ML)"
    - "Documentation includes methods, URLs, required headers (JWT), request payloads, and example responses"
  artifacts:
    - path: "backend/API_DOCS.md"
      provides: "Markdown-based API documentation for frontend devs"
      contains: "/api/v1/articles"
      min_lines: 50
  key_links: []
---

<objective>
Create comprehensive markdown documentation for the backend API.

Purpose: Provide a clear, readable reference for frontend developers outlining all available endpoints, required authentication, expected payloads, and responses. Replaces Swagger as per D-02.
Output: `backend/API_DOCS.md` file.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-api-polish-documentation/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate API_DOCS.md</name>
  <files>backend/API_DOCS.md</files>
  <read_first>.planning/phases/06-api-polish-documentation/06-CONTEXT.md</read_first>
  <action>
  Create `backend/API_DOCS.md` with detailed documentation for:
  
  **Base URL:** `http://localhost:5000/api/v1`

  **1. Authentication**
  - `POST /auth/login` (body: `username`, `password`) -> Returns JWT

  **2. Articles (Public)**
  - `GET /articles` (query: `page`, `limit`, `category`) -> Returns paginated list
  - `GET /articles/:id` -> Returns single article
  - `GET /articles/search?q=keyword` (query: `page`, `limit`) -> Returns paginated search results

  **3. Articles (Protected - Admin Only)**
  - Requires Header: `Authorization: Bearer <token>`
  - `POST /articles` (body: `title`, `description`, `content`) -> Auto-enriches via ML
  - `PUT /articles/:id` (body: `title`, `description`, `content`, `category`)
  - `DELETE /articles/:id`

  **4. ML Service Proxies (Protected - Admin/Frontend)**
  - Requires Header: `Authorization: Bearer <token>`
  - `POST /ml/classify` (body: `headline`, `short_description`)
  - `POST /ml/summarize` (body: `text`)
  - `POST /ml/similar` (body: `headline`, `short_description`, `k`)

  Include JSON examples for responses.
  </action>
  <verify>Check file is valid Markdown and contains all endpoints.</verify>
  <acceptance_criteria>
    - API_DOCS.md exists in the backend directory.
    - All 10 endpoints are documented.
    - JWT requirement is clearly stated for protected routes.
    - Query parameters and request bodies are documented.
  </acceptance_criteria>
  <done>Created API documentation in Markdown format.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Read `backend/API_DOCS.md` to ensure clarity and accuracy.
</verification>

<success_criteria>
- Frontend developers can read the markdown file and know exactly how to interact with the API.
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-polish-documentation/06-01-SUMMARY.md`
</output>
