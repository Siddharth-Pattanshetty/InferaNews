---
phase: 04-ml-service-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/mlService.js
autonomous: true
requirements: [ML-04]
user_setup: []
must_haves:
  truths:
    - "ML service is called using native fetch and ML_SERVICE_URL env var."
    - "If ML service fails or is unreachable, the wrapper catches the error, logs it, and returns null/defaults."
  artifacts:
    - path: "backend/src/services/mlService.js"
      provides: "Wrapper for classify, summarize, and similar ML endpoints."
      contains: "fetch"
      min_lines: 40
  key_links: []
---

<objective>
Implement the ML Service wrapper to securely communicate with the FastAPI microservice.

Purpose: Creates a robust HTTP client wrapper using native fetch to handle the three ML endpoints. It must handle timeouts/unreachable states gracefully by logging errors and returning safe default values (null) so the caller can degrade gracefully.
Output: `backend/src/services/mlService.js` file with `classifyText`, `summarizeText`, and `findSimilar` functions.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ml-service-integration/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mlService wrapper</name>
  <files>backend/src/services/mlService.js</files>
  <read_first>backend/src/config/env.js, .planning/phases/04-ml-service-integration/04-CONTEXT.md</read_first>
  <action>
  Create `backend/src/services/mlService.js`:
  ```javascript
  const { ML_SERVICE_URL } = require('../config/env');

  /**
   * Classify text into a category
   * @param {string} headline
   * @param {string} shortDescription
   * @returns {Promise<string|null>} category or null on failure
   */
  const classifyText = async (headline, shortDescription) => {
    try {
      const response = await fetch(`${ML_SERVICE_URL}/classify`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ headline, short_description: shortDescription }),
      });
      if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
      const data = await response.json();
      return data.category;
    } catch (error) {
      console.error('ML Service Error (classify):', error.message);
      return null;
    }
  };

  /**
   * Summarize text
   * @param {string} text
   * @returns {Promise<string|null>} summary or null on failure
   */
  const summarizeText = async (text) => {
    try {
      const response = await fetch(`${ML_SERVICE_URL}/summarize`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ text }),
      });
      if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
      const data = await response.json();
      return data.summary;
    } catch (error) {
      console.error('ML Service Error (summarize):', error.message);
      return null;
    }
  };

  /**
   * Find similar articles
   * @param {string} headline
   * @param {string} shortDescription
   * @param {number} k
   * @returns {Promise<Array|null>} array of results or null on failure
   */
  const findSimilar = async (headline, shortDescription, k = 5) => {
    try {
      const response = await fetch(`${ML_SERVICE_URL}/similar`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ headline, short_description: shortDescription, k }),
      });
      if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
      const data = await response.json();
      return data.results;
    } catch (error) {
      console.error('ML Service Error (similar):', error.message);
      return null;
    }
  };

  module.exports = {
    classifyText,
    summarizeText,
    findSimilar,
  };
  ```
  </action>
  <verify>Check file loads without error</verify>
  <acceptance_criteria>
    - mlService.js exports classifyText, summarizeText, findSimilar
    - mlService.js uses native fetch
    - Error handling returns null and logs to console
    - ML_SERVICE_URL is used for the endpoints
  </acceptance_criteria>
  <done>Created ML service wrapper using native fetch and graceful degradation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Ensure mlService.js is syntactically valid and uses fetch.
- [ ] Ensure it catches errors and degrades gracefully by returning null.
</verification>

<success_criteria>
- ML service wrapper is fully functional and handles potential failures safely.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ml-service-integration/04-01-SUMMARY.md`
</output>
