---
phase: 03-article-management
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/src/controllers/articleController.js
  - backend/src/routes/articleRoutes.js
  - backend/src/routes/index.js
autonomous: true
requirements: [ARTICLE-01, ARTICLE-02, ARTICLE-03, ARTICLE-04, ARTICLE-05, ARTICLE-07, ARTICLE-08]
user_setup: []
must_haves:
  truths:
    - "Admin can create article via POST /api/v1/articles (JWT required)"
    - "Anyone can fetch paginated article list via GET /api/v1/articles"
    - "Anyone can fetch single article via GET /api/v1/articles/:id"
    - "Admin can update article via PUT /api/v1/articles/:id (JWT required)"
    - "Admin can delete article via DELETE /api/v1/articles/:id (JWT required)"
    - "Article list sorted by createdAt descending (newest first)"
    - "Pagination response includes total, page, pages, limit"
    - "Category filter works via ?category=technology query param"
    - "Unauthenticated write attempts return 401"
  artifacts:
    - path: "backend/src/controllers/articleController.js"
      provides: "CRUD handlers with pagination, filtering, sorting"
      contains: "Article.find"
      min_lines: 80
    - path: "backend/src/routes/articleRoutes.js"
      provides: "Article route definitions with auth protection on writes"
      contains: "protect"
  key_links:
    - from: "backend/src/controllers/articleController.js"
      to: "backend/src/models/Article.js"
      via: "CRUD operations on Article model"
      pattern: "require.*models/Article"
    - from: "backend/src/routes/articleRoutes.js"
      to: "backend/src/middleware/auth.js"
      via: "protects write routes"
      pattern: "protect"
    - from: "backend/src/routes/index.js"
      to: "backend/src/routes/articleRoutes.js"
      via: "mounts at /articles"
      pattern: "use.*articles"
---

<objective>
Build the full article CRUD controller, routes, and mount them in the API.

Purpose: Implements all 7 remaining article requirements — create, read (list + single), update, delete with pagination, category filtering, date sorting, and admin protection on writes.
Output: Working article endpoints at /api/v1/articles.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-article-management/03-CONTEXT.md
@.planning/phases/03-article-management/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create article controller with all CRUD + pagination</name>
  <files>backend/src/controllers/articleController.js</files>
  <read_first>backend/src/models/Article.js, backend/src/controllers/authController.js, .planning/phases/03-article-management/03-CONTEXT.md</read_first>
  <action>
  Create `backend/src/controllers/articleController.js` with these handlers:

  **getArticles** (GET /api/v1/articles — public):
  - Parse query params: `page` (default 1), `limit` (default 10, max 50), `category`, `sort` (default '-createdAt')
  - If `category` param exists, add to filter: `{ category: req.query.category }`
  - Use `Article.find(filter).sort(sort).skip(skip).limit(limit)`
  - Count total with `Article.countDocuments(filter)`
  - Return: `{ success: true, data: articles, pagination: { total, page, pages: Math.ceil(total/limit), limit } }`

  **getArticle** (GET /api/v1/articles/:id — public):
  - `Article.findById(req.params.id)`
  - If not found: 404 `{ success: false, message: 'Article not found' }`
  - Return: `{ success: true, data: article }`

  **createArticle** (POST /api/v1/articles — protected):
  - Extract `title, description, content, category` from `req.body`
  - `Article.create({ title, description, content, category })`
  - Return 201: `{ success: true, data: article }`

  **updateArticle** (PUT /api/v1/articles/:id — protected):
  - `Article.findByIdAndUpdate(req.params.id, req.body, { new: true, runValidators: true })`
  - If not found: 404
  - Return: `{ success: true, data: article }`

  **deleteArticle** (DELETE /api/v1/articles/:id — protected):
  - `Article.findByIdAndDelete(req.params.id)`
  - If not found: 404
  - Return: `{ success: true, message: 'Article deleted' }`

  Wrap each handler in try/catch. On error: 500 `{ success: false, message: error.message }`.
  Limit is clamped: `Math.min(Math.max(parseInt(limit) || 10, 1), 50)`.
  </action>
  <verify>Check file loads without error</verify>
  <acceptance_criteria>
    - articleController.js contains "Article.find(" for list query
    - articleController.js contains "Article.findById(" for single fetch
    - articleController.js contains "Article.create(" for creation
    - articleController.js contains "Article.findByIdAndUpdate(" for update
    - articleController.js contains "Article.findByIdAndDelete(" for delete
    - articleController.js contains "Math.ceil(total / limit)" for pages calculation
    - articleController.js contains "{ createdAt: -1 }" or "'-createdAt'" for default sort
    - articleController.js contains "category" filter logic
    - articleController.js clamps limit to max 50
    - All handlers return { success: true/false } format
  </acceptance_criteria>
  <done>Article controller with 5 CRUD handlers, pagination, filtering, sorting</done>
</task>

<task type="auto">
  <name>Task 2: Create article routes with auth protection on writes</name>
  <files>backend/src/routes/articleRoutes.js</files>
  <read_first>backend/src/controllers/articleController.js, backend/src/middleware/auth.js, backend/src/routes/authRoutes.js</read_first>
  <action>
  Create `backend/src/routes/articleRoutes.js`:
  ```javascript
  const express = require('express');
  const {
    getArticles,
    getArticle,
    createArticle,
    updateArticle,
    deleteArticle,
  } = require('../controllers/articleController');
  const { protect } = require('../middleware/auth');
  const router = express.Router();

  // Public routes
  router.get('/', getArticles);
  router.get('/:id', getArticle);

  // Protected routes (admin only)
  router.post('/', protect, createArticle);
  router.put('/:id', protect, updateArticle);
  router.delete('/:id', protect, deleteArticle);

  module.exports = router;
  ```

  Key: GET routes are public (no `protect`), POST/PUT/DELETE use `protect` middleware.
  </action>
  <verify>Check file loads without error</verify>
  <acceptance_criteria>
    - articleRoutes.js contains "router.get('/', getArticles)"
    - articleRoutes.js contains "router.get('/:id', getArticle)"
    - articleRoutes.js contains "router.post('/', protect, createArticle)"
    - articleRoutes.js contains "router.put('/:id', protect, updateArticle)"
    - articleRoutes.js contains "router.delete('/:id', protect, deleteArticle)"
  </acceptance_criteria>
  <done>Article routes created with public reads and protected writes</done>
</task>

<task type="auto">
  <name>Task 3: Mount article routes in API aggregator</name>
  <files>backend/src/routes/index.js</files>
  <read_first>backend/src/routes/index.js</read_first>
  <action>
  Update `backend/src/routes/index.js`:
  - Add `const articleRoutes = require('./articleRoutes');` after the authRoutes require
  - Replace the comment `// router.use('/articles', articleRoutes);` with: `router.use('/articles', articleRoutes);`
  </action>
  <verify>Check that routes/index.js loads and has article routes mounted</verify>
  <acceptance_criteria>
    - backend/src/routes/index.js contains "require('./articleRoutes')"
    - backend/src/routes/index.js contains "router.use('/articles', articleRoutes)"
    - The placeholder comment is replaced with active code
  </acceptance_criteria>
  <done>Article routes mounted at /api/v1/articles</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 CRUD endpoints exist at /api/v1/articles
- [ ] GET routes are public, POST/PUT/DELETE are protected
- [ ] Pagination returns total, page, pages, limit
- [ ] Category filter works via query param
- [ ] Default sort is newest first
- [ ] All files load without syntax errors
</verification>

<success_criteria>
- All 7 article requirements addressed
- CRUD operations work with proper auth protection
- Pagination, filtering, and sorting implemented
- Routes mounted and accessible
</success_criteria>

<output>
After completion, create `.planning/phases/03-article-management/03-02-SUMMARY.md`
</output>
