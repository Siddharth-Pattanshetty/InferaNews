---
phase: 03-article-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/Article.js
autonomous: true
requirements: [ARTICLE-06]
user_setup: []
must_haves:
  truths:
    - "Article model has title, description, content as required fields"
    - "Category field uses enum validation with 9 known categories"
    - "Category defaults to 'uncategorized'"
    - "Summary and similarArticles fields exist with empty defaults"
  artifacts:
    - path: "backend/src/models/Article.js"
      provides: "Mongoose Article schema with enum categories"
      contains: "mongoose.model('Article'"
      min_lines: 20
  key_links: []
---

<objective>
Create the Article Mongoose model with enum categories and ML-ready fields.

Purpose: Defines the article data layer — the stable schema that both CRUD operations (Phase 3) and ML enrichment (Phase 4) build on. ML fields are defined now with defaults to avoid future schema migrations.
Output: Article model file ready for use by controllers.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-article-management/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Article Mongoose model</name>
  <files>backend/src/models/Article.js</files>
  <read_first>.planning/phases/03-article-management/03-CONTEXT.md, backend/src/models/Admin.js</read_first>
  <action>
  Create `backend/src/models/Article.js`:
  ```javascript
  const mongoose = require('mongoose');

  const CATEGORIES = [
    'politics',
    'entertainment',
    'technology',
    'sports',
    'business',
    'health',
    'science',
    'world',
    'uncategorized',
  ];

  const articleSchema = new mongoose.Schema(
    {
      title: {
        type: String,
        required: [true, 'Title is required'],
        trim: true,
      },
      description: {
        type: String,
        required: [true, 'Description is required'],
      },
      content: {
        type: String,
        required: [true, 'Content is required'],
      },
      // ML-enriched fields — populated by ML service in Phase 4
      category: {
        type: String,
        enum: CATEGORIES,
        default: 'uncategorized',
      },
      summary: {
        type: String,
        default: '',
      },
      similarArticles: [
        {
          type: mongoose.Schema.Types.ObjectId,
          ref: 'Article',
        },
      ],
    },
    { timestamps: true }
  );

  // Text index for search (Phase 5)
  articleSchema.index({ title: 'text', description: 'text', content: 'text' });

  module.exports = mongoose.model('Article', articleSchema);
  module.exports.CATEGORIES = CATEGORIES;
  ```

  Key decisions:
  - Export CATEGORIES array so controller can reference it for validation messages
  - Text index defined now for Phase 5 search (MongoDB creates it lazily)
  - ML fields (category, summary, similarArticles) have defaults — populated in Phase 4
  - timestamps: true adds createdAt/updatedAt for sorting (ARTICLE-07)
  </action>
  <verify>Run a temp script to verify model loads and has correct schema paths</verify>
  <acceptance_criteria>
    - backend/src/models/Article.js contains "enum: CATEGORIES"
    - backend/src/models/Article.js contains "'uncategorized'"
    - backend/src/models/Article.js contains "required: [true, 'Title is required']"
    - backend/src/models/Article.js contains "timestamps: true"
    - backend/src/models/Article.js contains "ref: 'Article'"
    - backend/src/models/Article.js contains "index({ title: 'text'"
    - backend/src/models/Article.js exports both model and CATEGORIES
  </acceptance_criteria>
  <done>Article model created with enum categories, ML-ready fields, and text index</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Article model loads without error
- [ ] CATEGORIES array has 9 entries
- [ ] Schema has title, description, content as required
- [ ] category defaults to 'uncategorized'
</verification>

<success_criteria>
- Article model created with all fields from D-01
- Enum validation on category field per D-03
- Text index defined for future search
</success_criteria>

<output>
After completion, create `.planning/phases/03-article-management/03-01-SUMMARY.md`
</output>
